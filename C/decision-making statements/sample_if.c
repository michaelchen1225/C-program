#include <stdio.h>
#include <stdlib.h>
int main()       
{
    int num;
    printf("enter a int:");
    scanf("%d",&num);

    if(num>0)
        printf("the int is greater than 0\n");
    printf("end...");

    return 0;
}